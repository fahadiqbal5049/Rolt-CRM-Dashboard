export interface StepProps {
  title: string;
  amount: string;
  count: number;
}

export interface StepDetailProps {
  contactName: string;
  phone: string;
  tags: string[];
  productName: string;
  amount: string;
  createdAt: string;
}
